version: 2

models:

  - name: mrt_orders_in_progress
    description: "Table analytics for the calculation of the order in progress by store"
    columns:
      - name: store_name,
        description: "Name of the store"

      - name: total_in_progress_order
        description: "Number of orders that are in progress"
        tests:
          - not_null